# Teaches kustomize that a Certificate's issuerRef is a name reference, so the
# namePrefix applied to the Issuer reaches it too.
nameReference:
- kind: Issuer
  group: cert-manager.io
  fieldSpecs:
  - kind: Certificate
    group: cert-manager.io
    path: spec/issuerRef/name
